{
  "name": "shared",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "source": "./src/index.ts",
  "version": "0.0.0",
  "scripts": {
    "dev": "tsc -w",
    "clean": "rm -rf .turbo && rm -rf node_modules && rm -rf dist",
    "build": "tsc"
  },
  "dependencies": {
    "tsconfig": "*"
  }
}

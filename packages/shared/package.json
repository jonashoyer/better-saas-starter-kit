{
  "name": "bs-shared-kit",
  "main": "./lib/index.js",
  "types": "./lib/index.d.ts",
  "sideEffects": false,
  "scripts": {
    "clean": "rm -rf lib",
    "build": "npm run clean && tsc",
    "watch": "tsc --watch"
  }
}
